using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum TeamId {
    PlayerTeam,
    EnemyTeam
};

public enum HabilityId {
    Attack,
    Magic,
    Shield,
    None = -1
};
